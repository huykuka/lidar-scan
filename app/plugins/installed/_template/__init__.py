"""
_template plugin — copy this directory, rename it (drop the leading _),
and fill in the TODOs.

Leading _ in the directory name tells discover_plugins() to skip this
package at startup so it never pollutes the live node registry.
"""
