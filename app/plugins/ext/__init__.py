# User-installed plugin packages live here.
# This directory is mounted as a Docker volume so plugins persist across image updates
# without shadowing the infrastructure code in app/plugins/__init__.py.
